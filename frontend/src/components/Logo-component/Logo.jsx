import { Typography } from '@mui/material';
import React from 'react'

// Logo component
export const Logo = () => (
  <Typography
    variant="h6"
    sx={{
      fontWeight: "bold",
      zIndex: 999,
      color: "#8B0000",
      position: "absolute",
      top: 20,
    }}
  >
    CRUDO.
  </Typography>
);

 
